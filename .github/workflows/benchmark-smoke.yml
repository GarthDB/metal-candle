name: Benchmark Smoke Test

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

env:
  RUST_BACKTRACE: 1
  CARGO_TERM_COLOR: always

jobs:
  benchmark-smoke:
    name: Benchmark Smoke Test (Regression Detection)
    runs-on: macos-14  # Apple Silicon M1
    permissions:
      pull-requests: write  # Required to post comments on PRs
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-bench-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Run benchmark smoke tests
        run: |
          echo "🔥 Running benchmark smoke tests (low sample size for speed)"
          echo "════════════════════════════════════════════════════════════"
          
          # Note: mlx_comparison requires Python/MLX, so we skip it in CI
          # Run other benchmarks to ensure they compile and execute
          
          # Check if benchmarks exist and run them
          if cargo bench --no-run 2>&1 | grep -q "no bench target"; then
            echo "⚠️  No benchmarks found to run"
            exit 0
          fi
          
          # Run benchmarks to verify they compile and execute
          # Note: We can't pass criterion-specific options via cargo bench --
          # Just verify benchmarks compile and don't panic
          cargo bench --all-features --no-run || {
            echo "⚠️  Benchmarks failed to compile"
            exit 1
          }
          
          echo "✅ Benchmarks compile successfully"
      
      - name: Report results
        if: always()
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════════"
          echo "⚠️  IMPORTANT: GitHub Actions Benchmark Limitations"
          echo "════════════════════════════════════════════════════════════"
          echo ""
          echo "These benchmarks run on shared hardware with ±10-20% variance."
          echo "They are ONLY for catching major performance bugs (>20% regression)."
          echo ""
          echo "For official performance numbers:"
          echo "  1. Run benchmarks locally on dedicated hardware"
          echo "  2. Follow the release checklist in docs/RELEASE_PROCESS.md"
          echo "  3. See docs/BENCHMARK_CI.md for methodology"
          echo ""

      - name: Post benchmark comment (on PR)
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ **Benchmark Smoke Test Complete**\n\n' +
                    'Verified that all benchmarks **compile successfully**. This smoke test ensures ' +
                    'benchmarks remain buildable but does not run performance measurements in CI.\n\n' +
                    '**Why not run benchmarks in CI?**\n' +
                    '- Shared GitHub Actions hardware has ±10-20% timing variance\n' +
                    '- Results are not suitable for performance claims\n' +
                    '- Would significantly slow down CI pipeline\n\n' +
                    '**For Official Benchmarks:**\n' +
                    'Run locally on dedicated hardware before release:\n' +
                    '```bash\n' +
                    './scripts/run_official_benchmarks.sh\n' +
                    '```\n\n' +
                    'See:\n' +
                    '- [Release Process](docs/RELEASE_PROCESS.md)\n' +
                    '- [Benchmark CI Strategy](docs/BENCHMARK_CI.md)\n' +
                    '- [Benchmark Methodology](BENCHMARKS.md)'
            })

